<shape android:shape="oval"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/bgColor" /> <!-- Background color when unchecked -->
    <stroke
        android:color="#382F54"
        android:width="20dp"/>
</shape>